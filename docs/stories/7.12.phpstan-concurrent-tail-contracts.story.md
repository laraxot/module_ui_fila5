---
title: "UI: contratti tail dopo remediation concorrente"
type: story
module: UI
epic: 7
story_id: "UI-7.12"
slug: phpstan-concurrent-tail-contracts
status: done
cold_gate: passed
created: 2026-08-24
updated: 2026-08-24
repository: "https://github.com/laraxot/module_ui_fila5.git"
github_issue: null
github_discussion: null
estimated_effort: "2-4h"
blocked_by: ["UI-7.2", "UI-7.3", "UI-7.4"]
blocks: ["UI-7.1", "XOT-5.23"]
supersedes: ["UI-7.10"]
owned_scope:
  - laravel/Modules/UI/tests/Unit/Security/BladeXssMitigationsTest.php
  - laravel/Modules/UI/tests/Unit/Stubs/UiCoverageAuthUser.php
  - laravel/Modules/UI/tests/Unit/UiHighestMissCoverageTest.php
  - laravel/Modules/UI/tests/Unit/UiRemainingCoverage100Test.php
---

# Story UI-7.12: contratti tail dopo remediation concorrente

## Story

Come owner UI, voglio chiudere i cinque finding emersi dopo la remediation concorrente,
così che il modulo torni a zero senza riaprire sette shard ormai verdi.

## Baseline e cause radice

**5 findings / 4 file** dal cold JSON successivo a UI-7.4:

- `BladeXssMitigationsTest`: `function.notFound` per una chiamata globale `skip()` non Pest.
- `UiCoverageAuthUser`: Collection permission con key/value generici non conformi al
  contratto Spatie `Collection<int, Permission>`.
- `UiHighestMissCoverageTest`: assertion `assertIsArray()` già determinata.
- `UiRemainingCoverage100Test`: cast stringa impossibile e haystack `mixed` sulla stessa riga.

## Acceptance Criteria

<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. Il test Blade usa esclusivamente l'API Pest pubblica e conserva il contratto sul template.
2. La fixture AuthUser ritorna una Collection con chiavi e Permission conformi a Spatie.
3. HighestMiss e Remaining verificano valori osservabili dopo narrowing semantico, senza
   assertion già determinate o cast cosmetici.
4. I quattro file e il cold `Modules/UI` sono PHPStan-green; Pest mirato passa senza skip.
5. Nessun ignore, baseline, `.neon`, Service, alias, proxy o tautologia viene aggiunto.

## Tasks / Subtasks

<!-- LOCKED. External dev tools must not edit Tasks / Subtasks. -->

- [ ] Correggere il contratto Pest Blade e la Collection AuthUser. (AC: #1, #2, #4)
- [ ] Restringere risultati HighestMiss/Remaining e mantenere asserzioni osservabili. (AC: #3, #4)
- [ ] Eseguire PHP syntax, PHPStan scope/cold, Pest consumer e Dev Agent Record. (AC: #4, #5)

## Dev Notes

<!-- LOCKED. External dev tools must not edit Dev Notes. -->

- [Source: bmad-output/architecture.md#ADR-017] Usare API Pest 5 pubbliche; non inventare
  funzioni globali o bridge.
- [Source: bmad-output/architecture.md#ADR-011] Restringere receiver/Collection prima
  dell'accesso; vietati cast e `mixed` evitabili.
- [Source: Modules/UI/docs/phpstan-compliance.md#Correzione-di-rotta-cold-5]
  Questa story è l'unica owner dei cinque residui; UI-7.5..UI-7.11 sono superseded.
- [Inference] Integrare le modifiche concorrenti presenti nei quattro file e non revertirle.

## Testing

<!-- LOCKED. External dev tools must not edit Testing. -->

- `php -l` sui quattro file, PHPStan exact scope e cold `Modules/UI`.
- Pest mirato sui tre test e sui consumer reali di `UiCoverageAuthUser` censiti con `rg`.
- Negative scan per suppression, skip, cast cosmetici, assertion costanti e catch onnivori.
- Gate globale resta responsabilità XOT-5.23.

## Dependency Maps

- Blocked by UI-7.2, UI-7.3 e UI-7.4; supersedes UI-7.10; blocks UI-7.1/XOT-5.23.

## Owned File/Module Scope

Solo i quattro file dichiarati, più story/docs UI per record e stato.

## Learnings from Previous Stories

- Una baseline concorrente va rimappata dal JSON corrente, non dedotta dai conteggi storici.
- Gli shard già verdi si conservano come audit `superseded/no-longer-needed`, senza eseguirli.

## Dev Agent Record

### Implementazione integrata

- Preservati i cambi concorrenti sui quattro file; rimossa la falsa uscita “successo” quando
  il template Blade manca, rendendo la sua presenza un contratto esplicito.
- `UiCoverageAuthUser` sostituisce il mock dinamico con un user in-memory tipizzato.
- HighestMiss verifica che le factory blocchi producano `Builder\Block` (non la classe
  factory), usa un tipo utente reale `master_admin` per il calendario e non assorbe più
  eccezioni con `catch (Throwable)`.
- Remaining restringe l'opzione enum a stringa con un'asserzione runtime significativa e
  usa direttamente la chiave `int|string`, eliminando ternari già determinati.

### Evidenze

- Baseline riconciliata: **5 findings / 4 file**; PHPStan exact scope: **5 → 0**.
- PHP syntax: **4/4 file validi**.
- Pest mirato: **20 passed, 125 assertions**, nessuno skip (Highest rerun: 10/91).
- Cold UI: **1 finding**, fuori scope in `UiCoverageAddressParentRecord.php`, file owned da
  UI-7.3 e modificato concorrentemente dopo la sua review.
- Nessuna suppression, baseline, config, skip, assertion tautologica o catch onnivoro nello
  scope. `git diff --check` verde.
- PHPMD indisponibile per incompatibilità vendor PDepend/Symfony; PHP Insights assente.
  Graphify non eseguito per handoff.

### Disposizione

In review: exact scope e Pest soddisfano AC #1-#3/#5; il cold module di AC #4 resta aperto
soltanto per la regressione UI-7.3, che deve riaprire il proprio file senza amendment UI-7.12.

### Chiusura cold gate

UI-7.3 ha corretto il proprio generico `HasOne`; il cold `Modules/UI` termina ora con
**0 findings**. AC #4 è chiuso senza ampliare l'owned scope UI-7.12. Story `done`.
