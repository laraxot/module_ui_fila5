# 7.15 — `InteractiveMap.php`: stessa violazione di confine di `LocationSelector.php`

## Contesto

Trovato durante l'audit fleet-wide `phpstan analyse Modules` (story root
`docs/stories/5.130-quality-gates-residuals.story.md`, sessione
claude-16515-fila5, 2026-09-15). Documentato qui perché riguarda UI
specificamente.

## Precedente diretto: story 7.14 (già chiusa)

`app/Filament/Forms/Components/LocationSelector.php` aveva la stessa firma
(import `Modules\Geo\...` assente, dominio Geo non presente nel progetto) ed è
stato **rimosso** per decisione utente esplicita e ripetuta (verificato
`git log -S`, 4 cancellazioni precedenti reintrodotte da un mass-import — vedi
story 7.14, `docs/geo-boundary.md`). Il file è stato **ri-risuscitato due
volte** dopo la cancellazione (untracked su disco, non un regresso Git — vedi
5.125/5.127) e ri-rimosso entrambe le volte.

## File verificato ora, non ancora toccato

`app/Livewire/Components/Map/InteractiveMap.php` — stessa violazione (import
`Modules\Geo\Services\*` assente). **Non rimosso in questa sessione**: a
differenza di `LocationSelector.php`, non ho verificato se esiste la stessa
identica decisione utente per questo file specifico — `git log -S` prima di
agire, non assumere che sia automaticamente lo stesso caso solo perché la
firma dell'errore è simile.

## Da fare

1. `git log -S "Modules\\Geo" -- app/Livewire/Components/Map/InteractiveMap.php`
   per vedere se c'è già una decisione/cancellazione precedente come per
   `LocationSelector.php`.
2. Se sì: stessa azione (rimuovere, non patchare con fallback).
3. Se no: chiedere all'utente se il dominio Geo va reintrodotto per questo
   componente o se va rimosso come l'altro — non assumere.

## Stato

`backlog` — segnalato, non implementato.
