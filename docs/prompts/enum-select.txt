bmad-create-story Agisci come un senior Laravel engineer specializzato in Filament v5 e Livewire v4 e devi progettare e implementare da zero un componente riutilizzabile chiamato EnumSelect nel namespace Modules\UI\Filament\Forms\Components seguendo rigorosamente le best practices ufficiali e producendo codice production-ready ultra DRY, KISS e clean code; prima di scrivere codice devi studiare attentamente e usare come riferimento primario le fonti ufficiali verificate Filament v5 Select docs https://filamentphp.com/docs/5.x/forms/select
 Filament v5 custom fields https://filamentphp.com/docs/5.x/forms/custom-fields
 Filament v5 forms overview https://filamentphp.com/docs/5.x/forms/overview
 Livewire v4 JavaScript https://livewire.laravel.com/docs/4.x/javascript
 Livewire v4 properties https://livewire.laravel.com/docs/4.x/properties
 Livewire v4 wire:model https://livewire.laravel.com/docs/4.x/wire-model
 Tom Select docs https://tom-select.js.org/docs/
 e devi anche studiare repository e codice reale da cui prendere spunto come Filament core repository https://github.com/filamentphp/filament
 Laravel framework https://github.com/laravel/framework
 e cercare esempi reali di enum UI patterns con Filament e PHP backed enums; il tuo obiettivo è costruire un componente EnumSelect che estende Filament\Forms\Components\Select e che accetta direttamente una enum PHP backed tramite ->enum(MyEnum::class) eliminando boilerplate nei form e centralizzando tutta la logica di mapping value label icon; il componente deve supportare enum che implementano Filament\Support\Contracts\HasLabel e Filament\Support\Contracts\HasIcon ma deve avere fallback robusti anche se queste interfacce non sono presenti usando il case name o metodi custom come label() e icon() se esistono; il componente deve usare native(false) perché in Filament v5 il Select avanzato usa Tom Select, deve usare allowHtml(true) quando necessario per permettere rendering HTML nelle option labels, deve generare automaticamente le options a partire da Enum::cases(), deve gestire correttamente la conversione tra value salvato e enum case tramite tryFrom senza errori e deve mantenere compatibilità con searchable(), preload(), multiple(), nullable(), required() e tutte le API standard del Select Filament; devi progettare con attenzione il rendering delle option perché Tom Select renderizza stringhe HTML e non Blade components, quindi se vuoi mostrare icone reali devi valutare e spiegare bene tre approcci possibili cioè semplice nome icona come stringa, HTML inline con classi CSS, SVG inline reale, scegliere quello più robusto e implementarlo in modo coerente; devi spiegare chiaramente best practices, bad practices e false friends: best practices centralizzare la logica enum nel componente, usare backed enums, usare HasLabel e HasIcon quando possibile, mantenere l’API del componente piccola e chiara, usare HtmlString solo quando davvero necessario, validare rigorosamente la enum class, mantenere il componente stateless lato UI perché lo state lo gestisce Filament; bad practices hardcodare labels e icone direttamente nei form, duplicare mapping enum in più punti, usare stringhe magiche sparse, non validare che la classe passata sia una backed enum, rompere la compatibilità con le API standard di Select; false friends pensare che ->options(MyEnum::class) mostri automaticamente anche le icone, pensare che HasIcon da solo basti a far renderizzare icone nella dropdown, pensare che si possano usare direttamente Blade components tipo x-filament::icon
 dentro le option labels di Tom Select, pensare che enum non backed funzionino uguale ai backed enum, pensare che HTML nelle option sia innocuo senza allowHtml(true), pensare che la logica debba stare nei form invece che nel componente; il componente deve offrire una fluent API pulita almeno con enum(), icons(), htmlLabels(), placeholderWhenNoSelection() e relativi getters, deve validare che la classe sia una enum esistente e backed usando enum_exists() e is_subclass_of(..., BackedEnum::class) e deve lanciare eccezioni chiare in caso di input invalido; devi includere anche esempi concreti di enum con HasLabel e HasIcon, esempio d’uso nel form con EnumSelect::make('type_id')->enum(TicketTypeEnum::class)->required()->searchable()->preload(), e devi spiegare esattamente perché il Select standard Filament non mostra icone anche se l’enum implementa HasIcon; devi produrre test Pest che validino almeno generazione options, fallback label, validazione enum invalida, conversione value a enum case, nomi colonna o configurazioni custom se introdotte, comportamento con htmlLabels on/off e icons on/off; devi scrivere il codice completo del componente pronto produzione nel namespace Modules\UI\Filament\Forms\Components e spiegarne le scelte architetturali in modo chiaro e tecnico prima di mostrare i file finali. documenta tutto dentro le cartelle docs dentro i moduli e dentro i temi , poi ingesta i nuovi documenti 